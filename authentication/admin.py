from django.contrib import admin
from .models import CustomUser

admin.site.register(CustomUser)  # Register the custom user model
# admin.site.register(JobDescription)  # Register the job description model
# admin.site.register(ApplicationStatus)  # Register the application status model


# Register your models here.
